# Sample NCC knowledge excerpts

Place markdown files here for the mock knowledge retrieval path (`USE_MOCK_KNOWLEDGE=true`).

In production, index authoritative NCC excerpts in Foundry IQ / Azure AI Search with extractive retrieval only.
